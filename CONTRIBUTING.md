# MemWeave 开发规范

## 1. 变更原则

- 先确认需求和设计，再实现代码。
- 每个变更只解决一个清晰的问题，避免无关重构。
- 影响公共接口、数据模型、一致性或安全边界的变更必须同步更新设计文档。
- 不把向量索引、模型输出或 Agent 自由推断当作唯一真相。

## 2. 仓库结构约定

~~~text
docs/       设计规格、架构决策和运行文档
src/        核心库和服务代码
tests/      自动化测试
examples/   最小可运行示例
adapters/   数据库、队列、模型和索引适配器
~~~

核心领域逻辑不得直接依赖具体数据库、模型、队列或搜索引擎；通过接口和适配器隔离。

## 3. 分支与提交

- 分支命名：feat/topic、fix/topic、docs/topic、refactor/topic。
- 提交信息使用 Conventional Commits：feat:、fix:、docs:、test:、refactor:、chore:。
- 一个 commit 保持单一意图；公共 API 和数据迁移不要与大规模格式化混在一起。
- 合并前必须保留可复现的测试命令和结果。

## 4. 代码质量

- 所有公共接口必须有类型、输入校验和错误语义。
- 事件、记忆对象和 API 响应必须带版本字段。
- 写入操作必须考虑幂等、并发冲突和重试。
- 记忆来源、权限作用域、版本和审计信息不能被省略。
- 任何模型生成内容默认视为不可信数据，不能绕过权限或工具校验。

## 5. 测试要求

- 新增行为先写失败测试，再写最小实现。
- 至少覆盖：正常路径、重复投递、乱序事件、旧版本覆盖保护、权限隔离和删除传播。
- 检索相关变更需要包含相关性、陈旧召回和 Token 预算测试。
- 异步任务必须有失败重试、dead-letter 和重放测试。

## 6. 数据与隐私

- 禁止提交密钥、Token、真实用户隐私和生产数据。
- 测试数据必须脱敏或合成。
- 任何新增敏感字段都必须说明保留期、访问权限和删除方式。
- 用户删除请求必须覆盖权威表、会话状态、缓存及向量/图索引。

## 7. 文档与架构决策

- 影响模块边界的决定记录在设计规格或 ADR 中。
- 变更设计后再修改实现计划，不在代码中隐式改变架构。
- 例子和注释说明“为什么”，而不仅是“做什么”。

## 8. 合并检查清单

- [ ] 需求和设计范围明确。
- [ ] 公共接口和数据模型已更新文档。
- [ ] 测试覆盖正常、失败、并发和权限路径。
- [ ] 没有敏感信息或未审计的跨作用域访问。
- [ ] 已运行格式检查、类型检查和测试。
- [ ] 提交信息符合约定，变更范围单一。
