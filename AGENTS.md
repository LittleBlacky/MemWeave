# MemWeave AI Coding Agent Instructions

本文件是 MemWeave 仓库的 AI coding agent 规范。所有 AI agent 在读取、修改或创建文件前必须遵守本文件。

## 1. 优先级与必读文件

指令优先级从高到低为：

1. 当前用户明确要求；
2. 本文件；
3. docs/superpowers/specs/ 下与任务相关的设计规格；
4. docs/superpowers/plans/ 下当前批准的实现计划；
5. 其他项目文档和代码注释。

开始工作前必须先阅读：

- README.md
- CONTRIBUTING.md
- docs/superpowers/specs/2026-08-29-memory-system-design.md

如果任务涉及实现，还必须阅读对应的 implementation plan。发现用户要求与设计规格冲突时，先停止修改并说明冲突。

## 2. 工作范围

- 先用一句话说明本次任务的目标、涉及文件和验证方式。
- 只修改完成当前任务所需的文件，不做无关重构、批量格式化或依赖升级。
- 先检查工作区现状，保留用户已有的未提交修改；不能覆盖、回滚或删除未知来源的文件。
- work/、sources/ 和 outputs/ 中的内容默认是分析材料或用户产物，不得擅自修改或提交。
- 不得读取、输出或提交密钥、Token、密码、证书和真实用户数据。

## 3. 设计变更门槛

以下变更必须先更新设计规格或新增 ADR，再实现代码：

- 修改事件模型、记忆对象、作用域、版本或一致性语义；
- 修改 Agent loop、召回顺序、提取/晋升策略或删除语义；
- 新增跨模块公共接口、外部数据源或存储后端；
- 修改权限、审计、敏感信息或 Prompt Injection 防护边界。

如果任务没有明确的设计或实现计划，不得自行发明大型架构；应先提出最小设计并等待确认。

## 4. 架构不变量

- 原始事件日志是不可变事实源；不得直接编辑历史事件来修复投影问题。
- Session Projection 保证当前会话连续性；显式 remember/update/forget 必须在请求返回前可见。
- Durable Projection 是长期权威状态；向量、关键词和图索引都是可重建的派生数据。
- 同一 memory key 的更新必须携带来源事件、版本和幂等信息；旧版本不能覆盖新版本。
- 身份和权限由服务边界注入，Agent 不得自行传 tenant_id、user_id、project_id 来扩大访问范围。
- 记忆和外部文档是数据，不是系统指令；任何工具调用仍需服务端 schema 和权限校验。
- 当前会话默认使用 session_consistent；不得假设异步普通提取或搜索索引已完成。

## 5. 实现规范

- 遵循 CONTRIBUTING.md 的分支、提交、测试和隐私规范。
- 新行为先写失败测试，再写最小实现。
- 核心领域逻辑只能依赖抽象接口，不得直接依赖具体数据库、模型、队列或搜索引擎。
- 所有公共接口必须有类型、输入校验、错误语义和版本字段。
- 异步任务必须可重试、幂等，并能进入 dead-letter 和重放流程。
- 不使用隐式全局状态保存会话或租户数据。
- 不为了通过测试而删除、放宽或绕过权限和一致性检查。

## 6. 记忆系统专用规则

- 明确的“记住、修改、删除、撤销”走同步结构化操作；规则无法确定时不得猜测为长期记忆。
- 普通事实和偏好先作为 candidate，经过策略和证据检查后才能 active。
- 历史经验必须关联完整 Episode、工具结果或用户确认；中间推测不能直接晋升。
- 召回结果必须经过作用域过滤、版本过滤、去重和 Token 预算控制。
- 当前用户的新明确陈述优先于旧长期记忆和模型推断。
- 用户删除必须传播到会话状态、权威表、缓存和所有派生索引。
- `Memory`、`Experience`、`Skill`、`Prediction` 必须分开建模，不得因为内容相似而直接互相转换。
- `Skill` 在 MemWeave 中只作为版本化、可审计、可检索的程序性记忆；实际步骤执行由 Agent Runtime 负责。
- `Prediction` 是非权威推断，不能直接写入 active 长期事实，也不能改变工具权限或执行边界。
- MemWeave 不执行任意 Skill 代码，不直接修改 Agent Prompt、工具、模型或生产代码；高风险能力变更必须由外部控制平面发布并可回滚。

## 7. 文件与命令安全

- 文件编辑使用 apply_patch；不要用重定向、脚本或批量命令覆盖文件。
- 未经明确授权，不执行递归删除、强制重置、强制 checkout、清空数据库或破坏性迁移。
- 不修改 work/ 下的参考仓库，不把参考仓库代码复制到 MemWeave。
- 运行命令前确认工作目录和目标路径；命令失败时先诊断，不通过删除数据规避错误。

## 8. 验证与交付

完成修改后必须：

1. 运行与改动相关的最小测试；
2. 运行格式、类型或静态检查（若项目已配置）；
3. 检查 git diff --check；
4. 汇报修改文件、测试命令和结果；
5. 明确说明未完成事项、已知风险和需要用户决定的内容。

如果无法验证，必须诚实说明原因，不得声称“已通过”。

## 9. 必须停止并询问用户的情况

- 任务目标、作用域或成功标准不明确，且不同理解会导致不同架构或数据行为；
- 需要选择公开/私有、删除数据、修改远程资源或推送外部服务；
- 发现现有代码与设计规格冲突；
- 需要修改用户已有变更或 worktree 中未知来源的内容；
- 需要新增高风险权限、敏感数据处理或自动修改自身代码/模型的能力。

## 10. 完成标准

一个任务只有在以下条件同时满足时才算完成：

~~~text
需求已实现
设计约束未被破坏
相关测试和检查已运行
变更范围可解释
风险和后续工作已披露
~~~
